# ========================================
# Git LFS Configuration for Atlas Services
# ========================================
# This file is automatically used when you enable volume persistence
# in your Atlas service repository.
#
# Git LFS (Large File Storage) allows you to store large files efficiently
# without bloating your git repository.
#
# SETUP:
#   git lfs install
#   git add .gitattributes
#   git commit -m "Setup Git LFS"
#
# MORE INFO:
#   https://git-lfs.github.com/
# ========================================

# NOTE: This is a general-purpose configuration.
# For service-specific presets, use:
#   source .github/lib/volume-persistence.sh
#   atlas_setup_gitattributes "database"  # or "media", "general"
# ========================================

# All files in data/ directory larger than 50MB will use LFS
# Adjust patterns below based on your service needs

# Common binary files
data/**/*.bin filter=lfs diff=lfs merge=lfs -text
data/**/*.dat filter=lfs diff=lfs merge=lfs -text

# Archives
data/**/*.tar filter=lfs diff=lfs merge=lfs -text
data/**/*.tar.gz filter=lfs diff=lfs merge=lfs -text
data/**/*.tar.bz2 filter=lfs diff=lfs merge=lfs -text
data/**/*.zip filter=lfs diff=lfs merge=lfs -text
data/**/*.7z filter=lfs diff=lfs merge=lfs -text

# Database files (uncomment if needed)
# data/**/*.db filter=lfs diff=lfs merge=lfs -text
# data/**/*.sqlite filter=lfs diff=lfs merge=lfs -text
# data/**/*.sql filter=lfs diff=lfs merge=lfs -text

# Backups (uncomment if needed)
# data/**/*.dump filter=lfs diff=lfs merge=lfs -text
# data/**/*.bak filter=lfs diff=lfs merge=lfs -text
